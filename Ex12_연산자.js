
        // +, - , *, /, %
      console.log(2+3); // 더하기
      console.log(3-2); // 빼기
      console.log(2*3); // 곱하기
      console.log(6/3); // 나누기
      console.log(7%4); // 나머지
      console.log(2**3); // 2의 3승(거듭제곱)
      console.log("------------------------")
      let num = 10;
      num += 5; // num = num + 5;
      console.log(num);


      // 증가 연산자
      let num2 = 10;
      let num3 = 10;
      let result = num2++; // 증가 시키기 전의 값을 전달
      let result2 = ++num3; // 증가 시킨 뒤의 값을 전달
      console.log(result);
      console.log(result2);

      // 감소 연산자
      let mnum2 = 10;
      let mnum3 = 10;
      let mresult = mnum2--; // 감소 시키기 전의 값을 전달
      let mresult2 = --mnum3; // 감소 시킨 뒤의 값을 전달
      console.log(mresult);
      console.log(mresult2);

